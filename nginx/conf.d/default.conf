# Upstream services
upstream backend {
    server backend:8000;
}

server {
    listen 80;
    server_name _;

    # ---- Portfolio Website ----
    location / {
        root /usr/share/nginx/html/portfolio;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # ---- React Frontend (static files built into this image) ----
    location /blackbird/ {
        alias /usr/share/nginx/html/blackbird/;
        index index.html;
        try_files $uri $uri/ /blackbird/index.html;
    }

    # ---- FastAPI Backend API ----
    location /blackbird/api/ {
        proxy_pass http://backend/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # SSE support (for scraper progress streaming)
        proxy_buffering off;
        proxy_cache off;
        proxy_set_header Connection '';
        proxy_http_version 1.1;
        chunked_transfer_encoding off;
        proxy_read_timeout 300s;
    }

    # ---- Health Check ----
    location /health {
        access_log off;
        return 200 'OK';
        add_header Content-Type text/plain;
    }
}

